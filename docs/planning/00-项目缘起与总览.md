# 金鳞·点石 本轮进展

## 已完成

1. **增量提交整理**
   - 修复 `.gitignore` 笔误（`.node_modules` → `node_modules/`），将 29,024 个依赖文件从版本库移除。
   - 把已验证但未提交的服务端改动拆分为 4 笔提交：
     - 板块日榜按 `rank` 排序并补齐 `and/asc` 导入
     - ingest 回填字典表 + 重算板块日榜
     - 原因补全接口迁出 admin 命名空间、区分全局/个人作用域
     - 完整品牌图标体系（ico/png/apple-touch/og-image）
   - 类型检查清零并固定 `typescript@5.8.2` / `vue-tsc@2.2.8`。
   - 补充 `docs/DEPLOY.md` 并更新 `README.md`。

2. **生产验证**
   - `npm run build` 通过，产物 **763 kB / 261 kB gzip**。
   - `npm run typecheck` 零错误。

3. **GitHub 推送**
   - 使用 `git filter-branch` 把 node_modules 从历史记录中彻底剔除（避免仓库肥胖）。
   - 强制推送到 `https://github.com/GuoSirius/king-scale-midas.git`，当前最新提交 `ec352fcf`。
   - GitHub 提示 2 个依赖漏洞（可在 Security → Dependabot 查看），不影响运行。

## 本地残留

- 工作区仍有 `.git-rewrite/` 临时目录（filter-branch 备份），因沙箱安全策略无法自动删除。
- 该目录只存在于本机，不影响 GitHub 克隆；用户可手动删除，或忽略。

## 后续建议

1. 按 `docs/DEPLOY.md` 创建 Cloudflare D1 并部署。
2. 配置 GitHub Secrets：`INGEST_URL`、`INGEST_SECRET`。
3. 处理 GitHub Dependabot 报告的 2 个依赖漏洞。
4. 完善 Python 采集器（akshare / pywencai 接入）。
